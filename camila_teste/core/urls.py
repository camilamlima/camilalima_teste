""" this module is urls """
from django.conf.urls import url
# from .views import request_post_save, iframe_checkout

urlpatterns = [
    # url(r'^orders/?$', request_post_save, name='post_save'),
    # url(r'^checkout/?$', iframe_checkout, name='iframe_checkout'),

]
